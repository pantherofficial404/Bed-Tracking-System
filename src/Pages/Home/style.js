import colors from '../../Theme/Colors';

const style = () => ({
  container: {
    display: 'flex',
    alignItems: 'center',
    flex: 1,
    flexDirection: 'column',
  },
});

export default style;